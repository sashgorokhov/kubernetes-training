apiVersion: v1
kind: ReplicationController
metadata:
  name: webapp-controller
spec:
  replicas: 1
  selector:
    name: webapp
  template:
    spec:
      containers:
        - name: webapp
          image: master:5000/webapp
          ports:
            - containerPort: 80
      #nodeSelector:
      #  node: node-1
    metadata:
      labels:
        name: webapp
        uses: db